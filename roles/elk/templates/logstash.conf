input {
  beats {
    port => 5044
  }
}

filter {
      grok {
        match => { "message" => "" }
      }
    }

output {
  elasticsearch {
    hosts => ["http://localhost:9200"]
    index => "%{[fields][component]}-%{+yyyy-MM-dd}"
  }
}
